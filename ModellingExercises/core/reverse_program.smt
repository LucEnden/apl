; Page 13 of modeling exercises:

; Consider the following program: 
; for i := 1 to 10 do 
;    if a > b then 
;       b := 2b; a := a – 3 
;    else 
;       a := 2a; b := b – 5 
;    fi 
; od 
; Find initial values for a and b such that afterwards a = 1000 and b = 999. 